import React from 'react';
import ReactDome from 'react-dom';

class Dashboard extends React.Component {
    render() {
        return(
            <div id="dashboard">
                Dashboard
            </div>
        );
    }
}